import React from 'react';
import classes from './HomepageFeatures.module.scss';

export default function HomepageFeatures(): JSX.Element {
  return (
    <section className={classes.features} />
  );
}
